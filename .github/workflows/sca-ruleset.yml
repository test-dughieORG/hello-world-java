name: SCA Ruleset Reconciliation

on:
  workflow_dispatch:
    inputs:
      action:
        description: "Add or remove repository from ORG SCA ruleset"
        required: true
        type: choice
        options:
          - add
          - remove

permissions:
  contents: read

jobs:
  sca-ruleset:
    runs-on: ubuntu-latest

    env:
      GH_TOKEN: ${{ secrets.GH_ADMIN_TOKEN }}
      ORG: dexcomit
      RULESET_ID: ${{ secrets.ORG_SCA_RULESET_ID }}
      REPO: ${{ github.repository }}

    steps:
      - name: Fetch ruleset
        run: |
          set -e
          curl -s \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/orgs/$ORG/rulesets/$RULESET_ID \
            > ruleset.json

      - name: Add repo to ORG SCA ruleset
        if: github.event.inputs.action == 'add'
        run: |
          set -e

          REPO_EXISTS=$(jq -r \
            --arg repo "$REPO" \
            '(.conditions.repository_name.include // []) | index($repo) != null' \
            ruleset.json)

          if [ "$REPO_EXISTS" = "true" ]; then
            echo "ℹ️  Repository $REPO already exists in SCA ruleset"
            exit 0
          fi

          jq \
            --arg repo "$REPO" \
            '
            .conditions.repository_name.include =
              ((.conditions.repository_name.include // []) + [$repo] | unique)
            ' ruleset.json > updated.json

          curl -X PATCH \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/orgs/$ORG/rulesets/$RULESET_ID \
            --data @updated.json

          echo "✅ Repository $REPO added to SCA ruleset"

      - name: Remove repo from ORG SCA ruleset
        if: github.event.inputs.action == 'remove'
        run: |
          set -e

          REPO_EXISTS=$(jq -r \
            --arg repo "$REPO" \
            '(.conditions.repository_name.include // []) | index($repo) != null' \
            ruleset.json)

          if [ "$REPO_EXISTS" = "false" ]; then
            echo "ℹ️  Repository $REPO is not in SCA ruleset"
            exit 0
          fi

          jq \
            --arg repo "$REPO" \
            '
            .conditions.repository_name.include =
              ((.conditions.repository_name.include // [])
               | map(select(. != $repo)))
            ' ruleset.json > updated.json

          curl -X PATCH \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/orgs/$ORG/rulesets/$RULESET_ID \
            --data @updated.json

          echo "✅ Repository $REPO removed from SCA ruleset"
